<Window x:Class="Курсовая.View.Windows.Allergies"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Аллергии" Height="700" Width="1000" MinHeight="730" MinWidth="850"
        WindowState="Maximized"
        Loaded="Window_Loaded">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <Button Style="{StaticResource BtnReturnstyle}" Content="Аллергии" Click="BtnReturn_Click"/>
        </StackPanel>

        <Border Style="{StaticResource BackInterface}" Padding="15" Margin="12" Grid.Row="1">
            <StackPanel>
                <TextBlock Text="Управление аллергиями" Style="{StaticResource TextUPUP}" HorizontalAlignment="Left"/>
                <TextBlock Text="Добавление и редактирование аллергий для вас и ваших подопечных" Style="{StaticResource HeaderTextStyle}"/>
            </StackPanel>
        </Border>

        <TabControl x:Name="AllergiesTabControl" Grid.Row="2" Margin="15" TabStripPlacement="Top"/>
    </Grid>
</Window>